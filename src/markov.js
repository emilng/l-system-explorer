import seedrandom from 'seedrandom';

function random() {

}

function choice(weights, choices) {
  const num = random();
  const sum = 0;
  const lastIndex = weights.length - 1;
  for(let i = 0; i < lastIndex; i++) {

  }
}

function markov(order, text) {
  const ngrams = {};

  for(let i = 0; i < text.length; i++) {
    text.substring
  }
}

export { markov };